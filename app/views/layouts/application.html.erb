<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>A Demo App for lilishuo</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><a href="/">FollowMe</a></h1>
        </li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <% if current_user %>
            <li><a href="">Welcome, <%= current_user.try(:display_name) %></a></li>
          <% else %>
          <li><a href="<%= new_session_path(:user) %>">Sign In</a></li>
          <li><a href="<%= new_registration_path(:user) %>">Sign up</a></li>
          <% end %>
        </ul>
      </section>
    </nav>

    <div class="container row">
      <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
